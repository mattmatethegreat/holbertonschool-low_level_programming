#include "main.h"

/**
 * main checks code
 *
 * Return: Always 0.
 */

int main(void)

{

	char *str;

	str = "I do nnot fear compuerts, I fear the lack of them - Isaac Asinov";
	_puts(str);
	return (0);
}
