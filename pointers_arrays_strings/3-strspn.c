#include "main.h"

/**
 * _strspn - entry
 * @s: input
 * @accept: input
 * Return:always 0 (Success)
 */

unsigned int _strspn(char *s, char *accept)

{

	unsigned int i, n, value, check;

	value = 0;

	for (i = 0; s[i] != '\0'; n++)
	{
		if (accept[n] != '\0'; n++)
		{
			if (accept[n] == s[i])
			{
				value++;
				checl = 1;
			}
		}
		if (check == 0)
	}
}
